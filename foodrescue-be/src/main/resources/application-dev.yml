spring:
  datasource:
    url: jdbc:mysql://127.0.0.1:3306/foodrescue?createDatabaseIfNotExist=true&useUnicode=true&characterEncoding=UTF-8&serverTimezone=UTC
    username: ${DB_USERNAME:root}
    password: ${DB_PASSWORD:11111111}

  jpa:
    show-sql: ${SHOW_SQL:true}
    hibernate:
      ddl-auto: ${HIBERNATE_DDL_AUTO:update}

app:
  seed:
    reset-demo-accounts: ${RESET_DEMO_ACCOUNTS:true}
  cors:
    allowed-origins: ${APP_CORS_ALLOWED_ORIGINS:http://localhost:3000,http://127.0.0.1:3000}
